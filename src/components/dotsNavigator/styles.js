import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    dots: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center',
        flex: 1
    }
})