import { StyleSheet } from 'react-native';
import {colors} from '../../assets/theme'

export default StyleSheet.create({
    base:{
        height: 50,
        
    },
    text: {
        fontSize: 20,
        fontWeight: '500',
        color: colors.gray
    },
    bigText: {
        fontSize: 26,
        fontWeight: '500',
        color: colors.gray
    }
})